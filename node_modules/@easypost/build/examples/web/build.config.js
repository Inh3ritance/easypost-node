import { web, production, development, plugins } from '../../src/webpackConfigs';

const config = {
  plugins: [
    ...web().plugins,
    plugins.assets('public'),
    plugins.cleanDir(),
    plugins.copydirs('assets'),
  ],
};

export default {
  production: production(web(config)),
  development: development(web(config)),
};
